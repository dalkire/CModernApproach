/*
 * Ch07 Ex04 | C Programming, A Modern Approach
 *
 * If c is a variable of type char, which one of the following statements
 * is illegal?
 * (a) i += c; // i has type int
 * (b) c = 2 * c - 1;
 * (c) putchar(c);
 * (d) printf(c);
 */
#include <stdio.h>

int main(void)
{
	return 0;
}

/*
 * (d) printf(c) is illegal because no formatted string is given.
 */
