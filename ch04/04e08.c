/*
 * Ch04 Ex08 | C Programming, A Modern Approach
 *
 * Would the upc.c program still work if the expression
 * 9 - ((total - 1) % 10) were replaced by (10 - (total % 10)) % 10?
 */
 #include <stdio.h>

int main(void)
{
	return 0;
}

/*
 * The expressions are equivalent for all totals > 0.
 */
